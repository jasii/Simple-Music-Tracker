__pycache__/
*.pyc
node_modules/
# Built SPA is a build artifact (produced by `npm run build` / the Docker image).
app/static/spa/
# So is tsc's incremental cache: it changes on every build and says nothing.
frontend/tsconfig.tsbuildinfo
.venv/
venv/
data/
*.db
*.db-wal
*.db-shm
.DS_Store
.agents/
skills-lock.json
.claude/
# Local dev script (not for repo)
dev.sh
music/